# 隐私说明

Orbit 采用本地优先设计。监测、聚合、统计和数据库存储默认在本机完成。

## 本地记录

程序可以保存：

- 前台应用和进程标识
- 窗口标题与活动时间戳
- 聚合后的键盘、鼠标活动数量
- 活跃、不活跃和监控连续性信息
- 媒体播放状态
- 浏览器历史中的 URL、域名、标题和访问时间
- 由上述数据派生的分类、任务、工作流、统计和报告

程序不保存：

- 按键内容
- 鼠标轨迹
- 剪贴板内容
- 屏幕截图
- 麦克风或系统音频
- 密码、Cookie、表单内容或浏览器登录态

## 浏览器数据

Chrome / Edge History、WAL 与 SHM 会复制到临时目录后只读扫描。程序不会修改浏览器原始数据库。完整网址只保存在本地证据中；发送给 AI 的工作流样本会进行限制和脱敏。

## 云 AI

AI 功能是可选的。用户选择 API 模式并配置提供商后，程序才会调用相应接口。API Key 保存到 Windows Credential Manager，不进入 SQLite 数据库或应用日志。

AI 请求只应包含当前分析需要的脱敏应用名、标题、域名、有限摘要和本地统计。完整路径、PDF 正文、密码、Cookie、表单和游戏证据不应发送给工作流 AI。

选择本机 Codex 模式时，任务通过本机 Codex CLI 执行，执行结果仍会进入本地审计和校验流程。

## 独立版数据位置

Orbit 1.3 独立版默认使用：

```text
%LOCALAPPDATA%\DailyTaskMonitorIndependent13\data\monitor.db
```

图标缓存位于：

```text
%LOCALAPPDATA%\DailyTaskMonitorIndependent13\icon-cache
```

该位置与 TimeWheel 和 Orbit 其他版本分离。

## 用户控制

用户可以：

- 主动暂停监测
- 排除应用或域名
- 人工修改活动分类
- 取消错误识别的自动工作流任务
- 导出报告
- 备份或删除本地数据库

提交 GitHub Issue 时，请勿上传数据库、API Key、凭据文件或包含私人窗口标题和完整网址的日志。
